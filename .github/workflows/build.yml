# on: [push, pull_request, workflow_dispatch]
on:
  pull_request:
    paths:
      - keyboards/crkbd/**
  push:
    paths:
      - keyboards/crkbd/**

name: crkbd firmware build


defaults:
  run:
    working-directory: ./keyboards/crkbd

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
