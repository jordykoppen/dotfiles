on:
  workflow_dispatch:
  push:
    paths:
      - keyboards/zmk/

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      config_path: keyboards/zmk/config/
      build_matrix_path: keyboards/zmk/build.yaml
      archive_name: zmk_firmware
